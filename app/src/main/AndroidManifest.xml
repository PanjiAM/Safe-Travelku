<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tubes_semester_5.safetravelku">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SafeTravelku">
        <activity android:name=".DetailWisata"></activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" />
        <activity
            android:name=".ScrollingDetailActivity"
            android:label="@string/title_activity_scrolling_detail"
            android:theme="@style/Theme.SafeTravelku.NoActionBar" />
        <activity
            android:name=".ScrollingDescripsiActivity"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/Theme.SafeTravelku.NoActionBar" />
        <activity android:name=".DaftarActivity" />
        <activity android:name=".MasukActivity" />
        <activity android:name=".HaloActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>